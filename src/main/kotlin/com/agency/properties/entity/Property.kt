package com.agency.properties.entity

import javax.persistence.*

/*@Entity
@Table(name = "property")
open class Property(
        @Id @GeneratedValue(strategy = GenerationType.AUTO) @Column(name = "property_id", nullable = false, unique = true) open val propertyId: Long? = null,
        @Column(name = "property_address", nullable = false) open val propertyAddress: String? = null,

        ) : BaseEntity()
        */
